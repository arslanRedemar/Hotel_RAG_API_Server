# System Common — 시스템 공통 요구사항

> **적용 범위**: Hotel AX 전체 솔루션 공통
> **작성일**: 2026-03-29
> **버전**: 2.0
> **참조**: 모든 기능 요구사항 문서에서 이 문서를 공통 기준으로 인용

---

## 1. 배경 및 맥락 (Context)

### 1-1. 공통 인프라가 필요한 이유

Hotel AX는 5개의 독립적인 기능 모듈(RAG, SOP, Work Order, Compliance, Revenue)로 구성된다. 이 모듈들이 각각 인증·알림·파일 저장·로깅을 따로 구현하면 다음 문제가 발생한다:

- **보안 취약점 산발**: 모듈별로 다른 인증 방식 → 누락 지점 발생
- **알림 중복/누락**: 각 모듈이 독립적으로 알림을 보내면 사용자가 같은 내용을 여러 번 받거나 아예 못 받음
- **감사 로그 불완전**: 기능별로 다른 로그 형식 → 통합 감사 시 수작업 취합 필요
- **개발 비효율**: 동일 기능을 5회 반복 구현

따라서 인증·권한·알림·파일 저장·감사 로그·배포는 공통 인프라로 중앙화하고, 각 모듈은 이를 사용한다.

### 1-2. 현재 운영 환경의 제약 조건

| 제약 | 내용 | 영향 |
|------|------|------|
| 인터넷 환경 | 호텔 내부 Wi-Fi는 간헐적으로 불안정 | 모바일 기능은 오프라인 지원 필요 |
| 기기 다양성 | 직원 기기: 개인 스마트폰(안드로이드·iOS), 프런트 PC, 공용 태블릿 | PWA 방식 지원, 특정 앱 불필요 |
| 외부 API 의존성 | OpenAI API는 외부 서비스 — 장애 시 RAG 기능 전체 영향 | Circuit Breaker 필수 |
| 개인정보 | 투숙객 데이터가 포함될 수 있는 문서 처리 | 데이터 마스킹·접근 제한 필수 |
| 감사 의무 | 법정 점검 기록은 최소 5년 보존 의무 | 삭제 방지 스토리지 정책 필요 |

---

## 2. 인증 및 권한 관리

### 2-1. 기능 요구사항

| ID | 요구사항 | 수락 기준 | 우선순위 |
|----|---------|---------|---------|
| SYS-F01 | 모든 API 엔드포인트는 JWT Bearer 토큰 인증을 요구해야 한다 (헬스체크·공개 엔드포인트 제외) | 토큰 없이 보호 엔드포인트 접근 시 401 반환 | P0 |
| SYS-F02 | Access Token 만료 시간은 1시간, Refresh Token은 7일로 설정해야 한다 | 만료된 토큰으로 요청 시 401 반환, Refresh Token으로 재발급 가능 | P0 |
| SYS-F03 | Refresh Token은 Redis에 저장되어 로그아웃 시 즉시 무효화(블랙리스트 처리)되어야 한다 | 로그아웃 후 기존 Refresh Token으로 재발급 시도 시 401 반환 | P0 |
| SYS-F04 | 사용자 역할은 Admin / Manager / Staff 3단계로 구분되어야 한다 | 역할별 권한 테이블의 모든 케이스에 대한 접근 제어 테스트 통과 | P0 |
| SYS-F05 | 비밀번호는 bcrypt (cost factor 12 이상)로 해시 저장해야 한다 | DB에서 평문 비밀번호를 조회할 수 없음, bcrypt 해시 형식 확인 | P0 |
| SYS-F06 | 로그인 실패 5회 이상 시 해당 계정을 30분 잠금 처리하고, 잠금 해제 시 이메일 알림을 발송해야 한다 | 6번째 시도 시 423 반환, 30분 후 자동 해제, 이메일 발송 확인 | P1 |
| SYS-F07 | 부서(department) 속성을 기반으로 데이터 접근 범위를 제한할 수 있어야 한다 | 하우스키핑 직원이 인사부서 문서 API 접근 시 403 반환 | P1 |

### 2-2. 역할별 기본 권한 매트릭스

| 역할 | RAG 검색 | SOP 관리 | Work Order | 점검 기록 | 수익 데이터 | 관리자 설정 |
|------|---------|---------|-----------|---------|-----------|-----------|
| Admin | 전체 부서 | 생성·수정·삭제 | 전체 조회·관리 | 전체 조회·보고서 | 전체 | 사용자·권한 관리 |
| Manager | 소속 부서 | 소속 부서 생성·수정 | 소속 부서 생성·조회 | 소속 부서 조회·서명 | 소속 부서 KPI | X |
| Staff | 소속 부서 | 읽기 전용 | 본인 생성·조회 | 본인 기록 | X | X |

---

## 3. 알림 시스템

### 3-1. 기능 요구사항

| ID | 요구사항 | 수락 기준 | 우선순위 |
|----|---------|---------|---------|
| SYS-F10 | 이메일(필수)과 웹 푸시(필수), SMS(선택) 채널을 지원해야 한다 | 이메일·웹 푸시 발송 각 1건 테스트 통과, SMS는 설정 시 동작 | P1 |
| SYS-F11 | 알림 유형별 수신 채널을 사용자가 계정 설정에서 직접 변경할 수 있어야 한다 | 설정 변경 후 해당 채널로만 알림 수신됨을 확인 | P2 |
| SYS-F12 | 알림 발송 실패 시 Celery 재시도로 최대 3회(지수 백오프 30초·90초·270초) 재시도하고 최종 실패는 이력에 기록해야 한다 | 의도적 발송 오류 주입 시 3회 재시도 후 `failed` 상태로 이력 저장 확인 | P1 |
| SYS-F13 | 모든 알림 발송 이력(수신자·채널·시각·성공여부·내용)이 90일 이상 조회 가능해야 한다 | GET /notifications/history?user_id={id} 가 90일 이내 이력 반환 | P2 |

### 3-2. 알림 트리거 정의

| 트리거 이벤트 | 수신 대상 | 채널 | 발송 조건 |
|------------|---------|------|---------|
| Work Order Critical 신고 | 시설팀 담당자 그룹 + Manager | 웹 푸시 + 이메일 | 즉시 (30초 이내) |
| Work Order SLA 초과 | 상위 Manager + Admin | 이메일 | SLA 마감 시각 도달 시 |
| Work Order 배정 수락 대기 초과 | 다음 순위 담당자 + Manager | 웹 푸시 | Critical 5분, High 15분 초과 시 |
| SOP 신규 버전 게시 | 해당 부서 직원 전원 | 웹 푸시 + 이메일 | 관리자 승인(게시) 즉시 |
| SOP 미확인 알림 | 미확인 직원 본인 | 이메일 | 게시 후 7일 미확인 시 |
| 점검 예정일 D-7, D-1 | 담당자 + Manager | 이메일 | 예정일 기준 오전 9시 |
| 점검 미완료 경고 | 담당자 + Manager | 이메일 | 예정일 오후 6시 미완료 시 |
| 반복 고장·NG 패턴 감지 | 관련 부서 Manager | 웹 푸시 + 이메일 | 패턴 감지 즉시 |
| 계정 잠금 | 해당 사용자 | 이메일 | 로그인 5회 실패 즉시 |

---

## 4. 파일 저장소

### 4-1. 기능 요구사항

| ID | 요구사항 | 수락 기준 | 우선순위 |
|----|---------|---------|---------|
| SYS-F20 | 업로드된 파일 원본은 서버 로컬 스토리지 또는 S3 호환 오브젝트 스토리지(MinIO)에 보관해야 한다 | 업로드된 파일이 지정 스토리지 경로에 존재함 확인 | P0 |
| SYS-F21 | 지원 형식: PDF, DOCX, TXT, JPEG, PNG / 최대 단일 파일 50MB | 50MB 초과 시 413 반환, 지원 외 형식 시 415 반환 | P0 |
| SYS-F22 | 파일 다운로드 URL은 유효 기간이 있는 Presigned URL로 발급되어야 하며, 직접 경로 접근은 차단되어야 한다 | 직접 파일 경로 접근 시 403, Presigned URL은 만료 후 접근 불가 확인 | P1 |
| SYS-F23 | 삭제 요청된 파일은 30일 보관(소프트 삭제) 후 영구 삭제 배치 작업으로 처리되어야 한다 | DELETE API 후 30일 이내 복구 API 동작 확인, 31일 후 영구 삭제 확인 | P1 |

---

## 5. 감사 로그 (Audit Log)

### 5-1. 기능 요구사항

| ID | 요구사항 | 수락 기준 | 우선순위 |
|----|---------|---------|---------|
| SYS-F30 | 모든 데이터 생성·수정·삭제 행위는 행위자·시각·대상 엔티티·변경 전후 값을 포함한 감사 로그로 기록되어야 한다 | 각 API 호출 후 audit_logs 테이블에 정의된 필드 포함 레코드 생성 확인 | P0 |
| SYS-F31 | 감사 로그는 수정·삭제가 불가능한 별도 테이블에 저장되어야 한다 | audit_logs 테이블에 UPDATE·DELETE 권한 없음 (DB 레벨 제한) 확인 | P0 |
| SYS-F32 | Admin은 날짜·행위자·대상 엔티티로 감사 로그를 필터링하여 조회할 수 있어야 한다 | GET /audit-logs?actor={email}&entity=work_order&from={date} 정확한 결과 반환 | P1 |
| SYS-F33 | 감사 로그는 최소 5년 보존해야 한다 | 5년 이내 레코드가 자동 삭제되지 않는 보존 정책 설정 확인 | P1 |

### 5-2. 감사 로그 스키마

```json
{
  "id": "audit-uuid-001",
  "actor_id": "user-uuid-123",
  "actor_email": "manager_kim@hotel.com",
  "actor_role": "manager",
  "action": "UPDATE",
  "entity_type": "work_order",
  "entity_id": "WO-2026-03001",
  "changes": {
    "before": { "status": "pending" },
    "after": { "status": "in_progress" }
  },
  "ip_address": "192.168.1.105",
  "user_agent": "Mozilla/5.0 ...",
  "created_at": "2026-03-29T09:20:00Z"
}
```

---

## 6. 외부 API 연동

### 6-1. 기능 요구사항

| ID | 요구사항 | 수락 기준 | 우선순위 |
|----|---------|---------|---------|
| SYS-F40 | OpenAI API 키는 서버 환경 변수로만 관리하며 로그·응답에 노출되어서는 안 된다 | 모든 API 응답·로그에서 `sk-` 패턴 문자열 없음 확인 | P0 |
| SYS-F41 | 외부 API(OpenAI, PMS, 날씨) 호출 시 Circuit Breaker 패턴을 적용하여 연속 실패 시 빠른 실패(Fail Fast) 처리해야 한다 | 외부 API 5회 연속 실패 시 circuit OPEN, 이후 요청 즉시 503 반환 확인 | P1 |
| SYS-F42 | Circuit Breaker 상태(OPEN/CLOSED/HALF_OPEN)와 오류율을 `/metrics` 엔드포인트에서 조회할 수 있어야 한다 | GET /metrics 응답에 각 외부 API별 CB 상태 포함 | P1 |
| SYS-F43 | PMS 연동은 REST API 또는 웹훅(Webhook) 방식을 지원해야 한다 | PMS Webhook 수신 엔드포인트 구현, 수신 시 DB 업데이트 테스트 통과 | P1 |
| SYS-F44 | 클라우드 API 호출 시 모델명·입력 토큰·출력 토큰·추정 비용을 `token_usage_logs` 테이블에 모듈별로 기록해야 한다 | 매 API 호출 후 모든 필드가 기록됨, GET /metrics/llm-cost 로 모듈별 집계 조회 가능 | P1 |

---

## 7. 배포 및 인프라

### 7-1. 기능 요구사항

| ID | 요구사항 | 수락 기준 | 우선순위 |
|----|---------|---------|---------|
| SYS-F50 | 애플리케이션은 Docker 이미지로 패키징되어야 한다 | `docker build` 성공, 이미지가 `docker run`으로 단독 실행 가능 | P0 |
| SYS-F51 | `docker-compose up` 단일 명령으로 API 서버·Celery Worker·MySQL·Redis 전체 스택이 실행되어야 한다 | 명령 실행 후 모든 서비스 헬스체크 통과 확인 | P0 |
| SYS-F52 | 환경 변수는 `.env` 파일로 관리하며, `.env.example`에 모든 필수 변수·설명·예시값이 문서화되어야 한다 | `.env.example`의 변수를 모두 설정하면 정상 구동됨 확인 | P0 |
| SYS-F53 | GitHub Actions CI에서 push 시 자동으로 린팅(ruff) + 단위 테스트(pytest)가 실행되어야 한다 | PR 생성 시 Actions 워크플로 자동 실행, 실패 시 머지 차단 | P1 |
| SYS-F54 | DB 스키마 변경은 Alembic 마이그레이션으로만 처리되어야 하며, 마이그레이션 이력이 버전 관리되어야 한다 | `alembic upgrade head` 성공, `alembic history`에 변경 이력 기록 확인 | P1 |

---

## 8. 모니터링 및 운영

### 8-1. 기능 요구사항

| ID | 요구사항 | 수락 기준 | 우선순위 |
|----|---------|---------|---------|
| SYS-F60 | `GET /health` 는 MySQL·ChromaDB·Redis 각 연결 상태와 전체 서비스 상태를 반환해야 한다 | 각 서비스 비정상 시 개별 컴포넌트에 `error` 표시, 전체 status는 `degraded` | P0 |
| SYS-F61 | 모든 API 요청·응답은 JSON 구조화 로그로 stdout 출력되어야 하며 `request_id`·`method`·`path`·`status_code`·`duration_ms`가 포함되어야 한다 | 각 요청 후 stdout 로그에 정의된 필드 포함 확인 | P1 |
| SYS-F62 | `GET /metrics` 는 경로별 호출 횟수, p50·p95 응답 시간, 오류율 및 Circuit Breaker 상태를 반환해야 한다 | 응답에 `api_stats`·`circuits` 두 섹션 포함 확인 | P1 |

---

## 9. 비기능 요구사항 공통 기준

| 항목 | 기준 | 측정 방법 |
|------|------|---------|
| API 응답 시간 (일반) | p95 기준 1초 이내 | 부하 테스트 50 동시 사용자 기준 |
| API 응답 시간 (LLM 포함) | 첫 토큰 3초 이내 | RAG 채팅 엔드포인트 p95 기준 |
| 시스템 가용성 | 월 99% 이상 | 헬스체크 엔드포인트 모니터링 |
| 보안 취약점 | OWASP Top 10 항목 대응 | 보안 체크리스트 리뷰 |
| 코드 테스트 커버리지 | 핵심 비즈니스 로직 80% 이상 | pytest --cov 보고서 기준 |
| 알림 발송 성공률 | 99% 이상 | 7일 발송 이력 모니터링 |

---

## 10. 기술 스택 (현재 구현 기준)

| 계층 | 기술 | 버전 | 비고 |
|------|------|------|------|
| API 서버 | FastAPI + Uvicorn | 0.115 / 0.30 | Python 3.11 |
| LLM (Tier 3, 클라우드) | OpenAI GPT-4o-mini | — | 복합 RAG 질의·장문 생성 |
| LLM (Tier 1~2, 로컬) | Ollama + Qwen2.5-3B / Phi-3-mini | — | 분류·구조 추출·단순 QA, CPU 서빙 가능 |
| 임베딩 (로컬 우선) | nomic-embed-text-v1.5 / BGE-M3 | — | 로컬 서빙, 클라우드 폴백 설정 가능 |
| OCR/비전 (로컬) | DeepSeek-VL2-Small / GOT-OCR2 | 1~2B | SOP 문서 이미지 파싱 전용 |
| 벡터 DB | ChromaDB (로컬 퍼시스턴트) | 1.0+ | |
| RDBMS | MySQL 8.0 | — | Docker |
| ORM | SQLAlchemy 2.0 + Alembic | — | |
| RAG 프레임워크 | LangChain 0.3 + LangGraph | — | LangSmith 트레이싱 지원 |
| 비동기 태스크 | Celery 5.4 + Redis 7 | — | 알림 발송·스케줄링 |
| 인증 | python-jose (JWT) + passlib (bcrypt) | — | |
| 파일 저장 | 로컬 / MinIO (S3 호환) | — | boto3 |
| 모니터링 | LangSmith (MCP 연동) | — | RAG 트레이싱 |

---

## 11. LLM 비용 최적화 — 로컬/클라우드 라우팅 전략

### 11-1. 배경

Hotel AX의 모든 AI 기능이 클라우드 API(OpenAI)에만 의존하면 두 가지 문제가 발생한다.

1. **비용**: 200실 호텔에서 직원 50명이 하루 평균 10회씩 AI를 사용한다고 가정하면, 월 15,000건 이상의 API 호출이 발생한다. OCR·분류처럼 소형 모델로도 충분한 작업까지 GPT-4o 급 API를 호출하면 불필요한 지출이 누적된다.
2. **가용성**: 인터넷 불안정·OpenAI 서비스 장애 시 OCR, 분류 등 기본 기능까지 전면 중단된다.

**해결 원칙: 로컬 우선(Local-First), 클라우드 폴백(Cloud Fallback)**

작업 복잡도에 따라 세 계층으로 모델을 분리하고, 하위 계층에서 처리 가능한 작업은 클라우드 API를 호출하지 않는다.

| 계층 | 대상 작업 | 모델 예시 | 클라우드 호출 조건 |
|------|---------|---------|---------------|
| Tier 1 — 로컬 전용 | OCR, WO 카테고리·긴급도 분류, 임베딩 생성 | DeepSeek-VL2-Small(1B), Phi-3-mini, nomic-embed-text | 신뢰도 < 임계값일 때만 폴백 |
| Tier 2 — 로컬 우선 | SOP 구조 추출, RAG 단순 질의(단일 청크 답변 가능) | Qwen2.5-3B, Gemma-2-2B | 신뢰도 < 70% 또는 멀티소스 필요 시 |
| Tier 3 — 클라우드 우선 | RAG 복합 질의(멀티소스 종합), 장문 생성 | GPT-4o-mini 이상 | 기본값 |

### 11-2. 기능 요구사항

| ID | 요구사항 | 수락 기준 | 우선순위 |
|----|---------|---------|---------|
| SYS-F70 | 시스템은 작업 유형에 따라 로컬 모델과 클라우드 API를 자동 선택하는 LLM Router 컴포넌트를 구현해야 한다 | 동일 요청이 설정된 라우팅 규칙에 따라 로컬/클라우드로 분기됨을 단위 테스트로 확인 | P1 |
| SYS-F71 | 로컬 모델 처리 결과의 신뢰도 점수가 설정 임계값(기본 70%) 미만인 경우에만 클라우드 API 폴백을 호출해야 한다 | 고신뢰도 샘플에서 클라우드 API 미호출 확인, 저신뢰도 샘플에서 폴백 호출 확인 | P1 |
| SYS-F72 | 로컬 모델과 클라우드 API의 모듈별·일별 사용 비율 및 클라우드 추정 비용을 대시보드에서 조회할 수 있어야 한다 | GET /metrics/llm-cost 가 `local_calls`, `cloud_calls`, `cloud_cost_usd_est` 모듈별 집계 반환 | P1 |
| SYS-F73 | 월 누적 클라우드 API 비용 추정치가 설정된 예산의 80%에 도달하면 Admin에게 경고 알림을 발송하고, 100% 초과 시 Tier 3 요청을 차단(또는 경고 후 허용)해야 한다 | 임계값 초과 시뮬레이션 시 알림 발송 이력 생성 확인 | P1 |
| SYS-F74 | 각 Tier의 로컬 모델 경로(또는 Ollama 엔드포인트)와 신뢰도 임계값을 환경 변수로 설정할 수 있어야 한다 | `.env`에서 `LOCAL_LLM_ENDPOINT`, `LOCAL_LLM_CONFIDENCE_THRESHOLD` 변경 후 즉시 반영 | P1 |
| SYS-F75 | 로컬 모델 서버(Ollama 등)가 응답하지 않을 경우 자동으로 클라우드 API로 전환하고, 전환 이벤트를 로그에 기록해야 한다 | 로컬 모델 중단 시뮬레이션 → 클라우드 폴백 동작 + `llm_fallback` 로그 생성 확인 | P1 |

### 11-3. 권장 로컬 모델 후보

| 용도 | 권장 모델 | 파라미터 | 비고 |
|------|---------|---------|------|
| OCR / 문서 이미지 파싱 | DeepSeek-VL2-Small, GOT-OCR2, Qwen2-VL-2B | 1~2B | GPU 불필요(CPU 추론 가능) |
| 텍스트 분류 (WO 카테고리·긴급도) | Phi-3-mini, Qwen2.5-1.5B | 1.5~3.8B | 파인튜닝 시 99% 이상 정확도 가능 |
| 구조 추출 (SOP) | Qwen2.5-3B, Gemma-2-2B | 2~3B | 한국어 지원 우선 |
| 임베딩 | nomic-embed-text-v1.5, BGE-M3 | — | 768/1024-dim, 로컬 서빙 가능 |
| 복합 QA (RAG) | Qwen2.5-7B, Gemma-2-9B | 7~9B | GPU 권장, CPU 서빙 시 응답 지연 허용 시 |

> 모델 선택은 서버 사양에 따라 조정. GPU 없는 환경은 1~3B 모델만 권장.

---

## 12. 연관 요구사항

- [RAG Assistant](./rag_assistant.md)
- [SOP Digitalization](./sop_digitalization.md)
- [Work Order Automation](./work_order_automation.md)
- [Compliance & Audit](./compliance_audit.md)
- [Revenue Management](./revenue_management.md)
